lifeops:
  services:
    user-service-url: ${USER_SERVICE_URL:http://localhost:8081}
    ai-service-url: ${AI_SERVICE_URL:http://localhost:8082}
    document-service-url: ${DOCUMENT_SERVICE_URL:http://localhost:8083}
    task-service-url: ${TASK_SERVICE_URL:http://localhost:8084}

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: user-service
              uri: ${lifeops.services.user-service-url}
              predicates:
                - Path=/api/users/**
            - id: ai-service
              uri: ${lifeops.services.ai-service-url}
              predicates:
                - Path=/api/ai/**
            - id: document-service
              uri: ${lifeops.services.document-service-url}
              predicates:
                - Path=/api/documents/**
            - id: task-service
              uri: ${lifeops.services.task-service-url}
              predicates:
                - Path=/api/tasks/**

          globalcors:
            cors-configurations:
              "[/**]":
                allowedOrigins:
                  - http://localhost:5173
                allowedMethods:
                  - GET
                  - POST
                  - PUT
                  - PATCH
                  - DELETE
                  - OPTIONS
                allowedHeaders:
                  - "*"
                allowCredentials: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

  endpoint:
    health:
      show-details: always

server:
  port: 8080

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    reactor.netty.http.client: DEBUG